import PropTypes from 'prop-types';

const Blok = ({blok}) => {

    console.log(blok)
    return (
        <div>
            
        </div>
    );
};

Blok.PropTypes = {
    blok:PropTypes.object.isRequired
}
export default Blok;